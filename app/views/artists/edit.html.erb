<h1>Editing Artist</h1>

<%= render 'form' %>

<hr><%= link_to image_tag('back.png')+"Back to " + @artist.flName, @artist %>
<br><%= link_to image_tag('back.png')+'Back to All Artists', artists_path %>
